name: Craftconomy3
version: ${version} b-${bamboo.repository.branch.name}-${bamboo.buildNumber}
description: ${project.description}
author: [ Greatman ]
website: ${project.url}

main: com.greatmancode.craftconomy3.SpoutLoader
load: POSTWORLD
platform: SERVER